aumentarPreco() {
    this.listaprodutos.forEach(produto => {
      produto.preco = produto.preco * 1.1;
    });
  }

  diminuirPreco() {
    this.listaprodutos.forEach(produto => {
      produto.preco = produto.preco * 0.9;
    });
  }

  aumentarPreco() {
    this.listaprodutos.forEach(produto => {
      // Usando toFixed(2) e convertendo para número com parseFloat
      produto.preco = parseFloat((produto.preco * 1.1).toFixed(2));
    });
  }

  diminuirPreco() {
    this.listaprodutos.forEach(produto => {
      // Usando toFixed(2) e convertendo para número com parseFloat
      produto.preco = parseFloat((produto.preco * 0.9).toFixed(2));
    });
  }